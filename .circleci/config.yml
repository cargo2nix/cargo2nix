version: 2
jobs:
  build:
    docker:
      - image: nixorg/nix:circleci
    resource_class: medium
    steps:
      - checkout
      # - restore_cache:
      #     key: nix-store
      # - run:
      #     name: Install cachix
      #     command: |
      #       nix-env -iA cachix -f https://cachix.org/api/v1/install
      - run:
          name: Build
          command: |
            echo $OMG_A_SECRET
            echo ${CACHIX_SIGNING_KEY:12:5}
            echo done
          no_output_timeout: 5h
      # - save_cache:
      #     key: nix-store
      #     paths:
      #       - /nix
